// Objeto Math 
let resultado;

resultado = Math.PI;
resultado = Math.round(2.7);
resultado = Math.ceil(2.2); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.8); // floor hacia abajo
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // convierte números negativos a positivos
resultado = Math.min( 3, 5,  1, 8, 2, 10); // el menor de una lista
resultado = Math.random();
resultado = Math.floor( Math.random() * 30);


console.log(resultado);